/*
package com.array;

//It is an way to sort the array in 0(logn)


public class MinHeap {

    int arr[];
    int size;
    int maxSize;

    public MinHeap(int maxSize){
        this.size=0;
        this.maxSize=maxSize;
        this.arr= new int[maxSize];
    }

    //In minheap there are some concept of parent ,left child, right child

    public boolean isLeaf(int index){
        if(index>maxSize/2 && index<maxSize)
            return true;

        return false;

    }

    public int leftChild(int index){
        arr[2*index+1];
    }

}
*/
